<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="../public/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600&family=Source+Serif+Pro:400,600&display=swap" rel="stylesheet">
    <style>
        /* ... your existing styles ... */

        /* Sort Options */
        .sort-options {
            max-width: 1270px;
            margin: 0 auto 30px;
            padding: 0 15px;
            display: flex;
            flex-wrap: nowrap;
            gap: 15px;
            align-items: center;
            overflow-x: auto;
        }
        .sort-options form {
            display: flex;
            flex-wrap: nowrap;
            gap: 15px;
            align-items: center;
            flex: 1;
        }

        .sort-options .search-input,
        .sort-options #searchBtn,
        .sort-options .animated-button {
            white-space: nowrap;
        }

        @media (max-width: 768px) {
            .sort-options {
                flex-direction: column;
                align-items: stretch;
            }
            .sort-options form {
                flex-direction: column;
                align-items: stretch;
            }
        }

        /* Keep rest of your styles unchanged */
    </style>
</head>
<body>
    <nav>
        <div class="container">
            <div class="logo">
                <a href="/"><img src="/images/logo.png" alt="Logo" ></a>
            </div>
            <div class="links">
                <a href="/">HOME</a>
                <a href="/allproducts">PRODUCTS</a>
                <a href="">ABOUT</a>
                <a href="">CONTACT</a>
                <a href="">ACCOUNT</a>
                <a href="" id="logout-button" class="logout-link">LOGOUT</a>
            </div>
            <div class="search">
                <!-- <i class="fas fa-search"></i> -->
            </div>
        </div>
    </nav>

    <h2 class="title">All Products</h2>

    <div class="sort-options">
        <form action="" method="post">
            <select name="category" id="category" class="form-select">
                <option value="All Categories">All Categories</option>
            </select>

            <select name="sortprice" id="sortprice" class="form-select">
                <option value="">All Prices</option>
                <option value="lowtoHight">Low to High</option>
                <option value="High to Low">High to Low</option>
            </select>

            <select name="priceRange" id="priceRange" class="form-select">
                <option value="">Price Range</option>
                <option value="100-1000">100-1000</option>
                <option value="1000-1500">1000-1500</option>
                <option value="1500-2000">1500-2000</option>
            </select>

            <select name="sortAlphabetically" id="sortAlphabetically" class="form-select">
                <option value="">Alphabetical Order</option>
                <option value="ascending">Ascending (A to Z)</option>
                <option value="descending">Descending (Z to A)</option>
            </select>

            <button type="submit" class="animated-button">Filter</button>
        </form>

        <input type="text" id="serchInput" class="search-input" placeholder="Search here...">
        <button id="searchBtn">Search</button>

        <form action="" method="get" id="clearFilterForm">
            <button type="submit" class="animated-button">Clear Filter</button>
        </form>
    </div>

    <div class="product-grid">
        <% if(productcollection && productcollection.length > 0) { %>
            <% productcollection.forEach(product => { %>
                <div class="product-item">
                    <div class="wishlist-container">
                        <a href="#"><i class="fa fa-heart wishlist-icon"></i></a>
                    </div>
                    <a href="/getproducts/<%=product._id%>">
                        <img src="<%=product.image[0] %>" alt="<%= product.productname %>">
                    </a>
                    <h6 class="product-name"><%= product.category.name %></h6>
                    <a href="/getproduct/<%= product._id %>">
                        <h4><%= product.productname %></h4>
                    </a>
                    <div class="rating">
                        <i class="fa fa-star"></i>
                        <i class="fa fa-star"></i>
                        <i class="fa fa-star"></i>
                        <i class="fa fa-star-half-o"></i>
                        <i class="fa fa-star-o"></i>
                    </div>
                    <p>₹<%= product.price %></p>
                    <a href="/getproduct/<%= product._id %>" class="btn">View Details</a>
                </div>
            <% }) %>
        <% } else { %>
            <div class="no-products">
                <h3 style="color: red;">No products found.</h3>
            </div>
        <% } %>
    </div>

    <footer class="simple-footer">
        <div class="footer-container">
            <div class="footer-cta">
                <h2>Ready for a next project?</h2>
                <p>Let's get started!</p>
                <div class="footer-actions">
                    <a href="#" class="btn">Enter</a>
                    <a href="#" class="btn btn-outline">Continue</a>
                </div>
            </div>
            
            <div class="footer-section">
                <h3>COLORLIB</h3>
                <ul>
                    <li><a href="#">Celebrate</a></li>
                    <li><a href="#">Buyer</a></li>
                    <li><a href="#">Supplier</a></li>
                </ul>
            </div>
            
            <div class="footer-section">
                <h3>Charging</h3>
                <ul>
                    <li><a href="#">Abstract</a></li>
                    <li><a href="#">Content</a></li>
                    <li><a href="#">Comment</a></li>
                </ul>
            </div>
            
            <div class="footer-section">
                <h3>Further Information</h3>
                <ul>
                    <li><a href="#">Terms & Conditions</a></li>
                    <li><a href="#">Privacy Policy</a></li>
                </ul>
            </div>
            
            <div class="footer-section">
                <div class="weather-widget">
                    <div class="temp">27°C</div>
                    <div class="condition">Partly cloudy</div>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.12/cropper.min.js"></script>
</body>
</html>
