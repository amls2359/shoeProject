<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Category Management</title>
    <style>
        /* General Styles */
        body { 
            font-family: Arial, sans-serif; 
            margin: 0;
            padding: 0;
            display: flex;
            min-height: 100vh;
            background-color: #f5f5f5;
        }
        
        /* Sidebar Styles */
        .sidebar {
            width: 215px;
            background-color: #333;
            color: white;
            padding: 20px;
            height: 100vh;
            position: fixed;
            box-shadow: 2px 0 5px rgba(0,0,0,0.1);
        }
        
        .sidebar h2 {
            text-align: center;
            color: white;
            margin-bottom: 30px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }

        .sidebar ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .sidebar ul li {
            margin: 15px 0;
        }

        .sidebar ul li a {
            color: #fff;
            text-decoration: none;
            display: block;
            padding: 8px 10px;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        .sidebar ul li a:hover {
            background-color: #444;
        }
        
        /* Main Content Styles */
        .main-content {
            flex: 1;
            margin-left: 250px;
            padding: 20px;
            background-color: white;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        
        .heading {
            flex-grow: 1;
            text-align: center;
            color: #333;
        }
        
        /* Table Styles */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        
        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }
        
        th {
            background-color: #f2f2f2;
            font-weight: bold;
            color: #333;
        }
        
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        
        /* Button Styles */
        .btn {
            padding: 6px 12px;
            border-radius: 4px;
            font-size: 14px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            transition: background-color 0.3s;
        }
        
        .btn-success {
            background-color: #4CAF50;
            color: white;
            border: none;
        }
        
        .btn-danger {
            background-color: #f44336;
            color: white;
            border: none;
        }
        
        .btn-edit {
            background-color: #2196F3;
            color: white;
            border: none;
        }
        
        /* Status Styles */
        .status-listed {
            color: green;
            font-weight: bold;
        }
        
        .status-unlisted {
            color: red;
            font-weight: bold;
        }
        
        /* Image Styles */
        .product-image {
            max-height: 50px;
            max-width: 50px;
            display: block;
        }
        
        /* Responsive Adjustments */
        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }
            
            .main-content {
                margin-left: 0;
            }
        }
        .btn-edit1
        {
   
            color:#f2f2f2;
            font-weight: bold;
            background-color: #4CAF50;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <h2>Admin Dashboard</h2>
        <ul>
            <li><a href="/admin/usermanagement">User Management</a></li>
            <li><a href="/admin/categorymanagement">Category Management</a></li>
            <li><a href="#">Product Management</a></li>
            <li><a href="#">Order Management</a></li>
            <li><a href="#">Coupon Management</a></li>
            <li><a href="#">Sales</a></li>
            <li><a href="#">Offer Management</a></li>
            <li><a href="/admin/adminLogin" id="logout-link">Logout</a></li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <div class="header-container">
            <h2 class="heading">Category Management</h2>
            <button id="add-category-btn" class="btn btn-success" style="padding: 8px 15px; font-weight: bold;">
                + Add Category
            </button>
        </div>
        
        <!-- Category Table -->
        <table id="category-table" class="table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Product Name</th>
                    <th>Category</th>
                    <th>Price</th>
                    <th>Image</th>
                    <th>Stock</th>
                    <th>Brand</th>
                    <th>status</th>
                    <th>List/UnList</th>
                    <th>Action</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <% products.forEach((products,index) => { %>
                    <tr>
                        <td><%= index+1 %></td>
                        <td><%= products.productname %> </td>
                        <td><%=products.category.name %> </td>
                        <td><%=products.price %></td>
                        <td>
                            <img style="max-height: 50px; max-width: 50px;" src="<%= products.image[0] %>" alt="no img">
                        </td>
                        <td><%=products.stock %> </td>
                        <td><%=products.brand %> </td>
                        <td>
                            <% if(products.islisted) { %>
                                <span class="status-listed">Listed</span>
                            <% } else { %>
                                <span class="status-unlisted">Unlisted</span>
                            <% } %>
                        </td>
                        <td>
                            <% if (!products.islisted) { %>
                                <a href="/admin/unListcategory/<%=products._id %>" class="btn btn-danger">UnList</a>
                            <% } else { %>
                                <a href="/admin/unListcategory/<%= products._id %>" class="btn btn-success">List</a>
                            <% } %>
                         </td>
                         <td>
                            <a href="/admin/editcategory/<%= products._id %>" class=" btn-edit1">Edit</a>
                            <a href="" class ="btn-edit2">Delete</a>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>